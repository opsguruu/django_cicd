version: '3.8'
services:
  django:
    build:
      context: .
      dockerfile: Dockerfile
    command: python /app/app/manage.py runserver 0.0.0.0:8001
    ports:
      - "8001:8001"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
